<!DOCTYPE html>
<html lang="en">
<head>
    <!-- head -->
    <%- include('partials/head') %> 
    <style>
        section {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        section h1 {
            font-size: 3em;
            animation: test 1s;
        }
        section h1 span {
            color: #A97E00;
        }
        section p {
            letter-spacing: 1em;
            animation: test 1.1s;
        }

        @keyframes test {
            0% {
                opacity: 0;
                transform: translateY(100px);
            }
            25% {
                opacity: 1;
                transform: translateY(-40px);
            }
        }

        @media(max-width: 768px) {
            section h1 {
                font-size: 2em;
                animation: test 1s;
            }
            section p {
                letter-spacing: 10px;
                animation: test 1.1s;
            }
        }
        @media(max-width: 576px) {
            section h1 {
                text-align: center;
                font-size: 1.5em;
                animation: test 1s;
            }
            section p {
                text-align: center;
                letter-spacing: 5px;
                animation: test 1.1s;
                font-size: 10px;
            }
        }
    </style>
</head>
<body>
    <!-- navbar -->
    <%- include('partials/navbar') %> 

    <section>
        <h1>Welcome to <span>UltraMovie</span> Website</h1>
        <p>Where you can See lots of Movies</p>
    </section>

    <!-- js codes -->
    <%- include('partials/js') %> 
</body>
</html>